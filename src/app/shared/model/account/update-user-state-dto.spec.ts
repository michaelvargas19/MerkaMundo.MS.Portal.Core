import { UpdateUserStateDTO } from './update-user-state-dto';

describe('UpdateUserStateDTO', () => {
  it('should create an instance', () => {
    expect(new UpdateUserStateDTO()).toBeTruthy();
  });
});
