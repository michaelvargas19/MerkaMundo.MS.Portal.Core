import { AspNetActionRoleDTO } from './asp-net-action-role-dto';

describe('AspNetActionRoleDTO', () => {
  it('should create an instance', () => {
    expect(new AspNetActionRoleDTO()).toBeTruthy();
  });
});
