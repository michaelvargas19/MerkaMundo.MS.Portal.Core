import { SalePaymentMethod } from './sale-payment-method';

describe('SalePaymentMethod', () => {
  it('should create an instance', () => {
    expect(new SalePaymentMethod()).toBeTruthy();
  });
});
