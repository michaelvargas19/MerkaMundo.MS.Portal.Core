import { CatalogProductPriceDto } from './catalog-product-price-dto';

describe('CatalogProductPriceDto', () => {
  it('should create an instance', () => {
    expect(new CatalogProductPriceDto()).toBeTruthy();
  });
});
