import { CatalogProductDto } from './catalog-product-dto';

describe('CatalogProductDto', () => {
  it('should create an instance', () => {
    expect(new CatalogProductDto()).toBeTruthy();
  });
});
